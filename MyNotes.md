1. Create Python env `uv venv`
2. Install ruby

```
sudo apt install rbenv
rbenv install -l
rbenv install 3.4.8
```
3. Install bundler `gem install bundler`
4. Install Local Setup `bundle install`
5. Run locally `bundle exec jekyll serve`
